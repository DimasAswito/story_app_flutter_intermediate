name: story_app_flutter_intermediate
description: "A new Flutter project for submission flutter intermediate on dicoding"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  http: ^1.2.1
  shared_preferences: ^2.2.3
  go_router: ^14.2.0
  image_picker: ^1.1.2
  google_maps_flutter: ^2.6.1
  geocoding: ^3.0.0
  geolocator: ^12.0.0
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  build_runner: ^2.4.11
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
